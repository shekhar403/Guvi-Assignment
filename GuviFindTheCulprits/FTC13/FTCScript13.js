let message;
let lock = 2;
//Dont change any code below this 
if (null && lock && undefined )
{
  message = "Go away";
}
else
{
 message = "welcome";
}
  console.log(message);


  // since lock is 2, if condition will be true. use && operator to make it false
